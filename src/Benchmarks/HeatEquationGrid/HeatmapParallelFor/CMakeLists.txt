if (BUILD_CUDA)
   CUDA_ADD_EXECUTABLE( benchmark_heat_parallel_for ../Base/HeatmapSolver.h main.h main.cu )
ELSE()
   add_executable( benchmark_heat_parallel_for ../Base/HeatmapSolver.h main.h main.cpp )
ENDIF()
